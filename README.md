# PR Reviewer Assignment Service

## Запуск через Docker

```bash
docker-compose up --build
```

или через make up

Сервис поднимется на `http://localhost:8080`.

## Конфигурация

Сервис использует переменную окружения `DB_DSN`, задающую строку подключения к PostgreSQL.
В `docker-compose.yml` она уже настроена.

## Миграции
Задание реализовано конкретно для поднятия через docker-compose-up(а не локально) для ускорения и удобства проверки работы,
в связи с этим реализована только одна up миграции, которую выполняется Postgres, поэтому миграции не разделены и не сделаны down миграции
При старте контейнера `db` автоматически применяется миграция `migrations/0001_init.sql`.

## Основные эндпоинты

* `POST /team/add` – создать команду с участниками.
* `GET /team/get?team_name=...` – получить команду.
* `POST /users/setIsActive` – установить флаг активности пользователя.
* `GET /users/getReview?user_id=...` – получить список PR, где пользователь назначен ревьювером.
* `POST /pullRequest/create` – создать PR и автоматически назначить до двух ревьюверов.
* `POST /pullRequest/merge` – пометить PR как MERGED (идемпотентно).
* `POST /pullRequest/reassign` – переназначить ревьювера на другого из его команды.
* `GET /stats`- получение статистики о pr юзеров.
* `POST /team/deactivate` - деактивация выбранных пользователей.

Формат ответов и ошибок соответствует `openapi.yml` из задания.


## Тестирование

 - реализованы unit-тесты
 - e2e тестирование

## Линтер
 
 - описана конфигурация линтера

## Команды

### 🐳 Управление Docker окружением
- make up — Сборка и запуск всего проекта (Приложение, БД, Swagger) в фоновом режиме.

- make down — Остановка всех сервисов и удаление томов (полная очистка базы данных).

- make logs — Просмотр логов запущенных контейнеров в реальном времени.

### 🧪 Тестирование
- make test — Прогон всех тестов (и Unit, и E2E).

- make test-unit — Запуск только Unit-тестов (проверка бизнес-логики, без базы данных).

- make test-e2e — Запуск интеграционных тестов в изолированном Docker-окружении (поднимает отдельную базу и приложение, тестирует сценарии, удаляет за собой).

### 🛠 Локальная разработка и инструменты
- make build — Компиляция бинарного файла приложения локально.

- make run-local — Запуск приложения локально (без контейнера), требует запущенной PostgreSQL на порту 5432.

- make lint — Запуск линтера (golangci-lint) для проверки стиля и ошибок в коде.

- make generate — Генерация моков для тестов (использует Docker, установка mockery не требуется).

- make clean — Удаление скомпилированного бинарного файла.